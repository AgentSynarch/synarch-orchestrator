{
  "name": "synarch-orchestrator",
  "version": "2.0.0",
  "description": "SYNARCH Orchestrator Agent — Advanced data pipelines with parallel steps, conditional branching, retry logic, and persistent DLQ.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node src/self-test.js",
    "validate": "node src/validate-pipeline.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "winston": "^3.13.0",
    "json-schema": "^0.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
